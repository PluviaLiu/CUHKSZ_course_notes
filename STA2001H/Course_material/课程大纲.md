generated by 薯塔AI
### **1. 概率导论 (Introduction to Probability)**

#### **① 定义与概念 (Definitions & Concepts)**
1.  **概率三元组 (Probability Triple):** $(\Omega, \mathcal{F}, \mathbb{P})$
    *   **样本空间 (Sample Space) $\Omega$**: 所有可能结果的集合.
    *   **事件集合 ($\sigma$-algebra) $\mathcal{F}$**: $\Omega$ 的子集构成的集合, 满足: (i) $\emptyset \in \mathcal{F}$, (ii) 若 $A \in \mathcal{F}$ 则 $A^c \in \mathcal{F}$, (iii) 若 $A_1, A_2, \dots \in \mathcal{F}$ 则 $\bigcup_i A_i \in \mathcal{F}$.
    *   **概率测度 (Prob. Measure) $\mathbb{P}$**: 函数 $\mathbb{P}: \mathcal{F} \to [0, 1]$, 满足: (i) $\mathbb{P}(\Omega) = 1$, (ii) 对互斥事件 $A_1, A_2, \dots$, 有 $\mathbb{P}(\bigcup_{i=1}^\infty A_i) = \sum_{i=1}^\infty \mathbb{P}(A_i)$ (可数可加性).
2.  **条件概率 (Conditional Probability):** $P(A|B) = \frac{P(A \cap B)}{P(B)}$, (要求 $P(B)>0$).
3.  **独立性 (Independence):**
    *   **两事件**: $A, B$ 独立 $\iff P(A \cap B) = P(A)P(B)$.
    *   **多事件**: $A_1, \dots, A_n$ 相互独立 $\iff P(\bigcap_{i=1}^n A_i) = \prod_{i=1}^n P(A_i)$.
    *   **成对独立 (Pairwise Indep.)**: $\forall i \neq j, P(A_i \cap A_j) = P(A_i)P(A_j)$. ⚠️ 相互独立比成对独立更强.
    *   **条件独立 (Conditional Indep.)**: $A, B$ 在 $C$ 下条件独立 $\iff P(A \cap B|C) = P(A|C)P(B|C)$.
4.  **划分 (Partition):** 集合族 $\{B_i\}$ 是 $\Omega$ 的划分, 若 $B_i$ 互斥 ($B_i \cap B_j = \emptyset, \forall i \neq j$) 且 $\bigcup_i B_i = \Omega$.

#### **② 公式与定理 (Formulas & Theorems)**
1.  **计数 (Counting):** 排列 (Permutation) $P(n,k) = \frac{n!}{(n-k)!}$, 组合 (Combinatic lion) $C(n,k) = \binom{n}{k} = \frac{n!}{k!(n-k)!}$.
2.  **基本性质 (Properties):**
    *   $P(A^c) = 1 - P(A)$.
    *   $A \subseteq B \implies P(A) \le P(B)$.
    *   $P(A \cup B) = P(A) + P(B) - P(A \cap B)$.
    *   **De Morgan's Laws:** $(\bigcup_i A_i)^c = \bigcap_i A_i^c$; $(\bigcap_i A_i)^c = \bigcup_i A_i^c$.
    *   **容斥原理 (Inclusion-Exclusion):** $P(\bigcup_{i=1}^n A_i) = \sum_i P(A_i) - \sum_{i<j} P(A_i \cap A_j) + \dots + (-1)^{n+1} P(\bigcap_{i=1}^n A_i)$.
3.  **核心定理 (Core Theorems):**
    *   **全概率公式 (Total Probability):** 若 $\{B_i\}$ 是划分, $P(A) = \sum_{i} P(A|B_i)P(B_i)$.
    *   **贝叶斯定理 (Bayes' Theorem):** $P(B_i|A) = \frac{P(A|B_i)P(B_i)}{P(A)} = \frac{P(A|B_i)P(B_i)}{\sum_{j} P(A|B_j)P(B_j)}$.
4.  **赌徒破产问题 (Gambler's Ruin):** 初始资金 $i$, 目标 $N$, 赢率 $p$. 最终获胜概率 $P_i$:
    $P_i = \begin{cases} \frac{i}{N} & \text{if } p = 1/2 \\ \frac{1 - ((1-p)/p)^i}{1 - ((1-p)/p)^N} & \text{if } p \neq 1/2 \end{cases}$

#### **③ ⚠️ 常见错误与混淆 (Common Mistakes & Confusions)**
1.  **排列 vs. 组合 (Permutation vs. Combination):** 排列关心顺序 (order matters), 组合不关心 (order doesn't matter).
2.  **独立 vs. 互斥 (Independent vs. Disjoint):** 若 $P(A), P(B) > 0$, 则 $A, B$ 互斥 $\implies$ $P(A \cap B) = 0 \neq P(A)P(B) \implies$ $A, B$ 不独立 (相关).
3.  **零概率 vs. 不可能事件 (Null vs. Impossible):** $P(A)=0$ (零概率) $\not\implies A=\emptyset$ (不可能). 连续 RV 取单点概率为0但可能发生.
4.  **辛普森悖论 (Simpson's Paradox):** 分组数据中的趋势在合并后可能逆转.

---
### **2. 离散型随机变量 (Discrete Random Variables)**

#### **① 定义与性质 (Definitions & Properties)**
1.  **随机变量 (Random Variable, RV):** 函数 $X: \Omega \to \mathbb{R}$.
2.  **分布函数 (CDF):** $F_X(x) = P(X \le x)$.
    *   性质: (i) 非降, (ii) 右连续, (iii) $\lim_{x\to-\infty} F(x) = 0$, (iv) $\lim_{x\to\infty} F(x) = 1$.
3.  **概率质量函数 (PMF):** $f_X(x) = P(X=x)$.
    *   性质: (i) $f(x) \ge 0$, (ii) $\sum_x f(x) = 1$.
    *   关系: $F(x) = \sum_{y \le x} f(y)$.
4.  **期望 (Expectation):** $E[X] = \sum_x x f(x)$.
    *   **线性性:** $E[aX + bY] = aE[X] + bE[Y]$.
    *   **函数期望:** $E[g(X)] = \sum_x g(x)f(x)$.
    *   **独立性:** $X, Y$ 独立 $\implies E[XY] = E[X]E[Y]$.
    *   **指示函数 (Indicator):** $E[I_A] = P(A)$.
5.  **方差 (Variance):** $Var(X) = E[(X - E[X])^2] = E[X^2] - (E[X])^2$.
    *   $Var(aX + b) = a^2 Var(X)$.
    *   $X, Y$ 不相关 $\implies Var(X+Y) = Var(X) + Var(Y)$.
6.  **矩生成函数 (MGF):** $M_X(t) = E[e^{tX}]$.
    *   $E[X^k] = M_X^{(k)}(0)$ (在0点的k阶导数).
    *   ⚠️ MGF 唯一确定分布 (若存在).

#### **② ⚠️ 常见分布比较 (Common Distribution Comparisons)**
1.  **二项 vs. 超几何 (Binomial vs. Hypergeometric):**
    *   $B(n,p)$: 有放回抽样, 独立重复试验, $p$ 不变.
    *   $Hy(N,K,n)$: 无放回抽样, 非独立试验, 成功概率改变.
2.  **几何 vs. 负二项 (Geometric vs. Negative Binomial):**
    *   $Ge(p)$: 首次成功所需的试验次数.
    *   $Ne(r,p)$: 第 $r$ 次成功所需的试验次数. $Ge(p)$ 是 $Ne(1,p)$ 的特例.
3.  **泊松近似二项 (Poisson Approx. to Binomial):**
    *   当 $n \to \infty, p \to 0$ 且 $np=\lambda$ (常数) 时, $B(n, p) \approx P(\lambda)$.

---
### **3. 连续型随机变量 (Continuous Random Variables)**

#### **① 定义与性质 (Definitions & Properties)**
1.  **概率密度函数 (PDF):** $f_X(x)$ 满足 $F_X(x) = \int_{-\infty}^x f(u)du$.
    *   性质: (i) $f(x) \ge 0$, (ii) $\int_{-\infty}^\infty f(x)dx = 1$.
    *   关系: $f(x) = F'(x)$.
    *   ⚠️ $f(x)$ 不是概率, $P(X=x)=0$.
2.  **概率计算:** $P(a \le X \le b) = \int_a^b f(x)dx$.
    *   ⚠️ 对于连续 RV, $P(a \le X \le b) = P(a < X < b)$.
3.  **期望 (Expectation):** $E[X] = \int_{-\infty}^\infty x f(x)dx$.
4.  **方差 (Variance):** $Var(X) = \int_{-\infty}^\infty (x-E[X])^2 f(x)dx = E[X^2] - (E[X])^2$.
5.  **矩生成函数 (MGF):** $M_X(t) = E[e^{tX}] = \int_{-\infty}^\infty e^{tx} f(x)dx$.
6.  **正态分布标准化 (Standardization of Normal):** 若 $X \sim \mathcal{N}(\mu, \sigma^2)$, 则 $Z = \frac{X-\mu}{\sigma} \sim \mathcal{N}(0, 1)$.
    *   $P(X \le x) = P(Z \le \frac{x-\mu}{\sigma}) = \Phi(\frac{x-\mu}{\sigma})$, 其中 $\Phi$ 是标准正态 CDF.

---
### **4. 联合与多维分布 (Joint & Multivariate Distributions)**

#### **① 定义与概念 (Definitions & Concepts)**
1.  **联合分布 (Joint Dist.):** $F(x,y) = P(X \le x, Y \le y)$.
    *   **离散 PMF:** $f(x,y) = P(X=x, Y=y)$.
    *   **连续 PDF:** $f(x,y)$ 使得 $F(x,y) = \int_{-\infty}^y \int_{-\infty}^x f(u,v)dudv$.
2.  **边缘分布 (Marginal Dist.):**
    *   **离散:** $f_X(x) = \sum_y f(x,y)$.
    *   **连续:** $f_X(x) = \int_{-\infty}^\infty f(x,y)dy$.
3.  **条件分布 (Conditional Dist.):** $f_{Y|X}(y|x) = \frac{f(x,y)}{f_X(x)}$ (要求 $f_X(x)>0$).
4.  **独立性:** $X, Y$ 独立 $\iff f(x,y) = f_X(x)f_Y(y) \iff F(x,y) = F_X(x)F_Y(y)$.
5.  **协方差 (Covariance):** $Cov(X,Y) = E[(X-\mu_X)(Y-\mu_Y)] = E[XY] - E[X]E[Y]$.
6.  **相关系数 (Correlation):** $\rho(X,Y) = \frac{Cov(X,Y)}{\sigma_X \sigma_Y}$, 其中 $-1 \le \rho \le 1$.
7.  **顺序统计量 (Order Statistics):** i.i.d. 样本 $X_1, \dots, X_n$ 的有序值 $X_{(1)} \le \dots \le X_{(n)}$.
    *   PDF of $X_{(j)}$: $f_{X_{(j)}}(x) = \frac{n!}{(j-1)!(n-j)!} [F(x)]^{j-1} [1-F(x)]^{n-j} f(x)$.
8.  **泊松过程 (Poisson Process):** 速率为 $\lambda$ 的计数过程 $\{N_t, t\ge0\}$.
    *   $N_0=0$.
    *   独立、平稳增量.
    *   $N_t \sim P(\lambda t)$.
    *   事件间隔时间 $T_i \stackrel{i.i.d.}{\sim} \mathcal{E}(\lambda)$.

#### **② 公式与定理 (Formulas & Theorems)**
1.  **全期望公式 (Law of Total Expectation):** $E[Y] = E[E[Y|X]]$.
2.  **条件方差公式 (Law of Total Variance):** $Var(Y) = E[Var(Y|X)] + Var(E[Y|X])$.
3.  **随机变量函数转换 (Change of Variables):**
    *   **一维:** $Y=g(X)$, $f_Y(y) = f_X(g^{-1}(y)) \left|\frac{d}{dy}g^{-1}(y)\right|$.
    *   **多维:** $(Y_1, Y_2) = T(X_1, X_2)$, $f_Y(y_1, y_2) = f_X(T^{-1}(y_1, y_2)) |J|$, $J$ 是 $T^{-1}$ 变换的雅可比行列式 (Jacobian).
4.  **正态相关分布 (Distributions related to Normal):** $Z_i \stackrel{i.i.d.}{\sim} \mathcal{N}(0,1)$.
    *   **卡方 Chi-squared:** $\sum_{i=1}^n Z_i^2 \sim \chi_n^2$.
    *   **t-分布:** 若 $Y \sim \chi_n^2$ 独立于 $Z_1$, 则 $\frac{Z_1}{\sqrt{Y/n}} \sim t_n$.
    *   **F-分布:** 若 $X \sim \chi_{d_1}^2, Y \sim \chi_{d_2}^2$ 独立, 则 $\frac{X/d_1}{Y/d_2} \sim F_{d_1, d_2}$.

#### **③ ⚠️ 常见错误与混淆 (Common Mistakes & Confusions)**
1.  **不相关 vs. 独立 (Uncorrelated vs. Independent):**
    *   独立 $\implies$ 不相关 ($Cov(X,Y)=0$).
    *   不相关 $\not\implies$ 独立. ⚠️ 反例: $X \sim \mathcal{N}(0,1), Y=X^2$.
    *   对于多维正态分布, 不相关 $\iff$ 独立.

---
### **5. 不等式与极限理论 (Inequalities & Limit Theorems)**

#### **① 不等式 (Inequalities)**
1.  **马尔可夫不等式 (Markov's):** 对非负 RV $X$ 和 $a>0$, $P(X \ge a) \le \frac{E[X]}{a}$.
2.  **切比雪夫不等式 (Chebyshev's):** 对任意 RV $X$ (均值 $\mu$, 方差 $\sigma^2$), $P(|X - \mu| \ge k) \le \frac{\sigma^2}{k^2}$.

#### **② 收敛模式 (Modes of Convergence)**
1.  **依概率收敛 (in Probability):** $X_n \xrightarrow{P} X$ if $\forall \epsilon > 0, \lim_{n\to\infty} P(|X_n - X| > \epsilon) = 0$.
2.  **依分布收敛 (in Distribution):** $X_n \xrightarrow{d} X$ if $\lim_{n\to\infty} F_{X_n}(x) = F_X(x)$ 在 $F_X$ 的所有连续点 $x$ 成立.
3.  **特征函数 (Characteristic Function, CF):** $\phi_X(t) = E[e^{itX}]$.
    *   $\phi_{X_n}(t) \to \phi_X(t) \ \forall t \implies X_n \xrightarrow{d} X$ (Lévy's Continuity Thm).

#### **③ 极限定理 (Limit Theorems)**
1.  **弱大数定律 (WLLN):** 若 $X_i$ i.i.d. 且 $E[|X_1|] < \infty$, 则样本均值 $\bar{X}_n = \frac{1}{n}\sum_{i=1}^n X_i \xrightarrow{P} E[X_1]$.
2.  **中心极限定理 (CLT):** 若 $X_i$ i.i.d. 且均值 $\mu$, 方差 $\sigma^2 < \infty$, 则 $\frac{\bar{X}_n - \mu}{\sigma/\sqrt{n}} = \frac{\sum X_i - n\mu}{\sigma\sqrt{n}} \xrightarrow{d} \mathcal{N}(0, 1)$.

#### **④ ⚠️ WLLN vs. CLT**
*   **WLLN:** 描述样本均值**收敛到一个常数** (总体均值).
*   **CLT:** 描述标准化后的样本均值的**分布**趋近于标准正态分布, 关注的是波动的形态.

---
### **附录: 分布表 (Distribution Tables)**
#### **表6.1: 离散分布 (Discrete Distributions)** ($q=1-p$)
| 分布 | PMF $f(x)$ | 支持集 $S_X$ | $E[X]$ | $Var(X)$ | MGF $M_X(t)$ | CF $\phi_X(t)$ |
| :--- | :--- | :--- | :--- | :--- |:--- |:--- |
| **Bernoulli($p$)** | $p^x q^{1-x}$ | $\{0, 1\}$ | $p$ | $pq$ | $q+pe^t$ | $q+pe^{it}$ |
| **Binomial($n,p$)** | $\binom{n}{x}p^x q^{n-x}$ | $\{0,...,n\}$ | $np$ | $npq$ | $(q+pe^t)^n$ | $(q+pe^{it})^n$ |
| **Poisson($\lambda$)** | $\frac{e^{-\lambda}\lambda^x}{x!}$ | $\{0,1,...\infty\}$ | $\lambda$ | $\lambda$ | $e^{\lambda(e^t-1)}$ | $e^{\lambda(e^{it}-1)}$ |
| **Geometric($p$)** | $q^{x-1}p$ | $\{1,2,...\infty\}$ | $1/p$ | $q/p^2$ | $\frac{pe^t}{1-qe^t}$ | $\frac{pe^{it}}{1-qe^{it}}$ |
| **Neg. Bin.($r,p$)** | $\binom{x-1}{r-1}p^r q^{x-r}$ | $\{r,r+1,...\}$ | $r/p$ | $rq/p^2$ | $(\frac{pe^t}{1-qe^t})^r$ | $(\frac{pe^{it}}{1-qe^{it}})^r$ |
| **Hypergeo.($N,K,n$)**| $\frac{\binom{K}{x}\binom{N-K}{n-x}}{\binom{N}{n}}$ | $\{\max(0,n-N+K),...,\min(n,K)\}$ | $n\frac{K}{N}$ | $n\frac{K}{N}(1-\frac{K}{N})\frac{N-n}{N-1}$ | N/A | N/A |

#### **表6.2: 连续分布 (Continuous Distributions)**
| 分布                          | PDF $f(x)$                                                                            | 支持集 $S_X$      | $E[X]$                        | $Var(X)$                                               | MGF $M_X(t)$                                    | CF $\phi_X(t)$                        |
| :-------------------------- | :------------------------------------------------------------------------------------ | :------------- | :---------------------------- | :----------------------------------------------------- | :---------------------------------------------- | :------------------------------------ |
| **Uniform($a,b$)**          | $\frac{1}{b-a}$                                                                       | $[a,b]$        | $\frac{a+b}{2}$               | $\frac{(b-a)^2}{12}$                                   | $\frac{e^{tb}-e^{ta}}{t(b-a)}$                  | $\frac{e^{itb}-e^{ita}}{it(b-a)}$     |
| **Exponential($\lambda$)**  | $\lambda e^{-\lambda x}$                                                              | $\mathbb{R}^+$ | $1/\lambda$                   | $1/\lambda^2$                                          | $\frac{\lambda}{\lambda-t}, t<\lambda$          | $\frac{\lambda}{\lambda-it}$          |
| **Gamma($\alpha,\lambda$)** | $\frac{\lambda^\alpha}{\Gamma(\alpha)}x^{\alpha-1}e^{-\lambda x}$                     | $\mathbb{R}^+$ | $\alpha/\lambda$              | $\alpha/\lambda^2$                                     | $(\frac{\lambda}{\lambda-t})^\alpha, t<\lambda$ | $(\frac{\lambda}{\lambda-it})^\alpha$ |
| **Normal($\mu,\sigma^2$)**  | $\frac{1}{\sqrt{2\pi\sigma^2}}e^{-\frac{(x-\mu)^2}{2\sigma^2}}$                       | $\mathbb{R}$   | $\mu$                         | $\sigma^2$                                             | $e^{\mu t + \sigma^2t^2/2}$                     | $e^{i\mu t - \sigma^2t^2/2}$          |
| **Beta($\alpha,\beta$)**    | $\frac{\Gamma(\alpha+\beta)}{\Gamma(\alpha)\Gamma(\beta)}x^{\alpha-1}(1-x)^{\beta-1}$ | $[0,1]$        | $\frac{\alpha}{\alpha+\beta}$ | $\frac{\alpha\beta}{(\alpha+\beta)^2(\alpha+\beta+1)}$ | N/A                                             | N/A                                   |
| **Chi-sq.($k$)**            | $\mathcal{G}(k/2, 1/2)$                                                               | $\mathbb{R}^+$ | $k$                           | $2k$                                                   | $(1-2t)^{-k/2}, t<1/2$                          | $(1-2it)^{-k/2}$                      |

π